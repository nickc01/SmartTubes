if ServerCore ~= nil then return nil end;

--Declaration

--Public Table
ServerCore = {};
local ServerCore = ServerCore;

--Variables

--Functions

--Sets up the values to be synced under the groupName
function ServerCore.SetupSyncedValues(GroupName,...)
	local Values = {...};
	for _,value in ipairs(Values) do
		
	end
end